#we importing usefull functions we want to use in our programm 
import usefull_tools 

#now we can use all functions from usefull_tools.py file 
#for example roll_dice function 
print(useful_tools.roll_dice(10))

#there is a huge database of already
#written modules, we can use it to save so much 
#time cos there is a big chance that someone has 
#already made that we are trying to 
#there are build in modules that are included by default
# and external modules 
#It can be found on the internet 