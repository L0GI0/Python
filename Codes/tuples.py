#!/usr/bin/python3
#similar to lists but has few key differences from lists 

#creating tuples (coordinates)

#tuples are immutable (niezmienny, staly, nieodwolany)
#once you create a tuple ii cannot be modified  
coordinates = (4, 5)
#first index 
print(coordinates[0])
#second index 
print(coordinates[1])

#coordinates[1] = 10 #this gives an error 
#lists is the same as tuples but tuples cannot be changed after
#declaration 

#list of tuples

coordinates = [(4,5), (6,7), (80, 34)]

#the most of the time lists are used 